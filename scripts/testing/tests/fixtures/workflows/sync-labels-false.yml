---
name: Labeler

on:
  pull_request:

permissions: {}

jobs:
  label:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/labeler@v6
        with:
          sync-labels: false
...
